public class Scope {
    public static void main(String[] args) {
        //anything initialized outside block can be used inside
        //anything initialized inside block cannot be used outside

        //anything initialized outside block cannot be initialized inside the block again.
        //anything initialized inside block can be initialized again outside the block


    }
}
